$(document).ready( function() { app.initialized() .then(function(_client) { var client = _client; client.data.get('loggedInUser').then (function(data) { if(data['loggedInUser']['contact']['email'] == 'arvind.mohan@freshworks.com') { client.interface.trigger('hide', {id: 'tag'});client.interface.trigger('hide', {id: 'group'});client.interface.trigger('hide', {id: 'ticketEdit'}); } }); }); });